# Archived Launcher Note

The old launcher collection was moved to `../deprecated/slurm/`.

The active benchmark run path lives in the sibling repo:

- `../input-representation-benchmark/slurm/`

This directory is kept only as a pointer so the top-level layout stays readable.
